# Cursor Rules -- agentscaffold

Generated by AgentScaffold. For the full agent operating rules see **AGENTS.md** at the project root.

---

## Quick Reference

- **AGENTS.md** is the authoritative source of truth for all agent rules.
- Read AGENTS.md and this file before every task.
- Plans live in `docs/ai/plans/`. Templates in `docs/ai/templates/`.
- Architecture: `docs/ai/system_architecture.md` (READ-ONLY for agents).
- Workflow state: `docs/ai/state/workflow_state.md`.

---

## Key Prohibitions

- Emojis are **forbidden** everywhere (code, comments, docstrings, markdown, logs, tests, config).
- Do NOT execute plans with incomplete review checklists.
- Do NOT skip dependency verification.
- Do NOT create interfaces without contracts.
- Do NOT modify `docs/ai/system_architecture.md` without human approval.

---

## Planning Rules (Summary)

1. Any architectural or refactor work MUST begin with a plan file.
2. Plans use templates from `docs/ai/templates/`.
3. Plans must include: File Impact Map, Tests, Execution Steps, Validation, Rollback.
4. Plan lifecycle: Draft -> Review -> Ready -> In Progress -> Complete.
5. Devil's advocate review required before execution.
6. Expansion review required before execution.
7. Retrospective required after execution.

---

## Standards

Follow implementation standards in `docs/ai/standards/`:

| Standard | Required |
|----------|----------|
| errors | Yes |
| logging | Yes |
| config | Yes |
| testing | Yes |

---

## Architecture

- 6-layer architecture defined in `docs/ai/system_architecture.md`.
- Components MUST consume output from their upstream layer.
- No cross-layer bypassing allowed.

---

## Testing

- Every feature or bug fix MUST include corresponding tests.
- Tests written before or alongside code (test-first or test-alongside).
- Follow patterns in `docs/ai/standards/testing.md`.
